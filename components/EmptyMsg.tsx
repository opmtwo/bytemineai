const EmptyMsg = ({ msg }: { msg: string }) => (
	<div className="panel-block is-block">
		<p className="title is-4 m-0 p-5 has-text-centered has-text-grey">{msg}</p>
	</div>
);

export default EmptyMsg;
